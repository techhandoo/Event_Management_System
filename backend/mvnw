#!/bin/sh
# Maven Wrapper startup script
set -e

MAVEN_VERSION="3.9.6"
MAVEN_HOME="${HOME}/.m2/wrapper/dists/apache-maven-${MAVEN_VERSION}"
MAVEN_URL="https://repo.maven.apache.org/maven2/org/apache/maven/apache-maven/${MAVEN_VERSION}/apache-maven-${MAVEN_VERSION}-bin.zip"

if [ ! -d "$MAVEN_HOME" ]; then
    echo "Downloading Maven ${MAVEN_VERSION}..."
    mkdir -p "$MAVEN_HOME"
    curl -fsSL "$MAVEN_URL" | unzip - -d "${HOME}/.m2/wrapper/dists/" > /dev/null 2>&1
    mv "${HOME}/.m2/wrapper/dists/apache-maven-${MAVEN_VERSION}"/* "$MAVEN_HOME/" 2>/dev/null || true
fi

exec "$MAVEN_HOME/bin/mvn" "$@"
