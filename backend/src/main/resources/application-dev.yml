spring:
  datasource:
    url: jdbc:mysql://localhost:3306/event_manager?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: eventmanager
    password: eventmanager123

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  kafka:
    bootstrap-servers: localhost:9092

  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.eventmanager: DEBUG
    org.springframework.security: DEBUG
    org.springframework.kafka: DEBUG
