spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/event
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true

logging:
  level:
    com.eventmanager: DEBUG
    org.springframework.security: DEBUG
